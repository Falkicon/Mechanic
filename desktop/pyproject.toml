[build-system]
requires = ["setuptools==83.0.0", "wheel==0.46.3"]
build-backend = "setuptools.build_meta"

[project]
name = "mechanic-desktop"
version = "0.2.1"
description = "Local companion tool for WoW addon development built on AFD"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "GPL-3.0" }
authors = [
    { name = "Falkicon" }
]
keywords = ["wow", "warcraft", "addon", "development", "afd"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: GNU General Public License v3 (GPLv3)",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: MacOS",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Games/Entertainment",
    "Topic :: Software Development :: Build Tools",
]
dependencies = [
    "click>=8.0",
    "fastapi>=0.100",
    "uvicorn[standard]>=0.20",
    "watchfiles>=0.20",
    "pydantic>=2.0",
    "python-dotenv>=1.0.0",
    "requests>=2.28",
]

[project.optional-dependencies]
# MCP server support (for mech mcp command)
mcp = [
    "mcp>=1.0.0",
    "fastmcp>=0.1.0",
]
# Research commands (Gemini API)
research = [
    "google-genai>=0.8.0",
]
# Asset management (PNG to TGA conversion)
assets = [
    "Pillow>=10.0.0",
]
# All extras
all = [
    "mcp>=1.0.0",
    "fastmcp>=0.1.0",
    "google-genai>=0.8.0",
    "Pillow>=10.0.0",
]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "httpx>=0.27",
    "ruff>=0.4",
]

[project.urls]
Homepage = "https://github.com/Falkicon/Mechanic"
Repository = "https://github.com/Falkicon/Mechanic"
Documentation = "https://github.com/Falkicon/Mechanic#readme"

[project.scripts]
mechanic = "mechanic.cli:main"
mech = "mechanic.cli:main"

[tool.setuptools]
package-dir = {"" = "src", "mechanic.dashboard" = "dashboard"}
packages = ["mechanic", "mechanic.commands", "mechanic.dashboard", "afd", "afd.core", "afd.server", "afd.testing", "afd.transports"]

[tool.setuptools.package-data]
"mechanic.dashboard" = ["*.html", "*.js"]

[tool.ruff]
extend-exclude = ["src/_afd_OLD"]

# CI deliberately gates the stable correctness baseline while the broader
# modernization/style cleanup is handled separately.  The 2026-09-05 review
# baseline recorded 962 findings under its default rule set (mostly UP, I,
# and RUF modernization rules); that deferred inventory is recorded in
# docs/quality-review-2026-09-05.md rather than silently discarded.
[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

