{
  "$schema": "https://mechanic.dev/schemas/libs.json",
  "description": "Bootstrap libraries for !Mechanic - minimal set that must load first",
  "mode": "include",
  "libraries": {
    "LibStub": "latest",
    "MechanicLib": "local"
  },
  "notes": {
    "LibStub": "Required for MechanicLib registration",
    "MechanicLib": "Internal library - registration API for other addons"
  }
}
